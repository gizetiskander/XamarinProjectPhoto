<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="XamarinProjectPhoto.Views.MainPage" Visual="Material" NavigationPage.HasNavigationBar="False">
    <StackLayout>
        <ListView x:Name="listview_Projects" Margin="0,0" ItemsSource="{Binding Projects}" SelectedItem="{Binding}" RowHeight="50"
                SeparatorColor="Gray">
            <ListView.ItemTemplate>
                <DataTemplate>
                    <Label Text="{Binding Name}" TextColor="Black"/>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </StackLayout>

</ContentPage>
